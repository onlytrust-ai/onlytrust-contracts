{
  "name": "onlytrust-contracts",
  "version": "1.0.0",
  "description": "OnlyTrust smart contracts — escrow, manifest registry",
  "scripts": {
    "compile": "npx hardhat compile",
    "test:hardhat": "npx hardhat test",
    "deploy:sepolia": "npx hardhat run scripts/deploy.ts --network baseSepolia",
    "deploy:base": "npx hardhat run scripts/deploy.ts --network base"
  },
  "keywords": [],
  "author": "OnlyTrust",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.10",
    "@nomicfoundation/hardhat-verify": "^3.0.16",
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^17.3.1",
    "hardhat": "^3.4.3"
  }
}
